# name: Lint

# on: [push, pull_request]

# jobs:
#   lint:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - uses: psf/black@stable
#         with:
#           options: "--check --verbose"
#           src: "./src"
#           version: "21.5b1"

name: black-action
on: [push, pull_request]
jobs:
  linter_name:
    name: runner / black formatter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: rickstaa/action-black@v1
#         with:
#           black_args: ". --check"
      - run: |
          python3 -m black src/plotting
